{
  "name": "ui-kit-mcp-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "node scripts/bundle-registry.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
